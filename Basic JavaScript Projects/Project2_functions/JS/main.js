function Dysfunction () {
    var sentence = "I am learning"; //Variable 1
    sentence += " a lot from this book!"; //what we're adding to var sentence
    document.getElementById("Concatenate").innerHTML = sentence; //This searches for a element that has been contactentaed and uses it to replace the existing element
}